<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f7f6;
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      color: #333;
    }

    .login-container {
      background: #fff;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      width: 90%;
      max-width: 380px;
      text-align: center;
      box-sizing: border-box;
    }

    .login-container img.logo {
      display: block;
      margin: 0 0 30px;
      width: 48px;
      height: auto;
    }

    .error {
      text-align: left;
      color: #e74c3c;
      font-weight: bold;
      margin-bottom: 15px;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    label {
      display: block;
      text-align: left;
      font-size: 14px;
      margin-bottom: 6px;
    }

    label .required {
      color: #3862ff;
      margin-right: 4px;
    }

    input[type="text"],
    input[type="password"] {
      padding: 12px;
      font-size: 16px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      width: 100%;
      box-sizing: border-box;
      margin-bottom: 20px;
    }

    button[type="submit"] {
      padding: 12px;
      font-size: 16px;
      background-color: #3862ff;
      color: #fff;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 100%;
      box-sizing: border-box;
    }

    button[type="submit"]:hover {
      background-color: #2f54e7;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <!-- logo from your URL -->
    <img
      src="https://imgs.search.brave.com/3t6DKoPQP_VzrlsiV__uq0d_5JUDZPVRqPzU2pKDkoc/rs:fit:860:0:0:0/g:ce/aHR0cHM6Ly9zd2Mu/aWl0Zy5hYy5pbi9w/bGFjZW1lbnQtc3Rh/dHMvc3RhdGljL2lt/YWdlcy9zd2NfaWNv/bi5zdmc"
      alt="Logo"
      class="logo"
    />

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="<%= BASE_PATH %>/api/image/login" method="post">
      <label for="username">
        <span class="required">*</span>Email
      </label>
      <input
        type="text"
        id="username"
        name="username"
        placeholder="Email"
        required
      />

      <label for="password">
        <span class="required">*</span>Password
      </label>
      <input
        type="password"
        id="password"
        name="password"
        placeholder="Password"
        required
      />

      <button type="submit">Login</button>
    </form>
  </div>
</body>
</html>
